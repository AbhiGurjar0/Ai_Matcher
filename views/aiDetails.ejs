<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Review - <%= model.name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 text-white flex justify-center items-center min-h-screen p-4">
    <div class="max-w-3xl w-full bg-gray-800/70 backdrop-blur-lg p-6 rounded-lg shadow-lg">
        
        <!-- AI Model Info -->
        <div class="text-center">
            <h2 class="text-3xl font-bold text-green-400"><%= model.name %></h2>
            <p class="text-gray-300 mt-2"><%= model.description %></p>
            <p class="text-yellow-400 font-semibold mt-2">⭐ Average Rating: <%= model.rating.toFixed(1) %> / 5</p>
        </div>

        <!-- User Reviews Section -->
        <h3 class="text-xl font-bold text-white mt-6">User Reviews</h3>
        <div class="space-y-4 mt-4">
            <% if (model.reviews.length === 0) { %>
            <p class="text-gray-400 text-center">No reviews yet. Be the first to review!</p>
            <% } else { %>
            <% model.reviews.forEach(review => { %>
            <div class="bg-gray-700 p-4 rounded-lg shadow-md transform transition-transform hover:scale-105">
                <p class="text-white font-bold"><%= review.user %></p>
                <p class="text-yellow-400 text-lg">⭐ <%= review.rating %>/5</p>
                <p class="text-gray-300"><%= review.comment %></p>
                <p class="text-gray-500 text-sm"><%= new Date(review.date).toLocaleString() %></p>
            </div>
            <% }) %>
            <% } %>
        </div>

        <!-- Review Form -->
       
    </div>
</body>

</html>
